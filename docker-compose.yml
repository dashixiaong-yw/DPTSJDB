services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        BUILD_VERSION: v1.27
    container_name: dptsjdb-1.27
    restart: unless-stopped
    ports:
      - "3080:3080"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - DOCKER_BUILDKIT=1
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3080/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
